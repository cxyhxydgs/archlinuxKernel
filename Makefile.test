# 测试程序编译
test: test_module.c
	gcc -o test_module test_module.c

# 清理测试程序
clean_test:
	rm -f test_module

# 完整清理
clean: clean_test
	make -C /lib/modules/$(shell uname -r)/build M=$(PWD) clean

# 编译所有
all: test
	make -C /lib/modules/$(shell uname -r)/build M=$(PWD) modules